const splashScreen = document.getElementById('splashScreen'); const authScreen = document.getElementById('authScreen'); const chatContainer = document.getElementById('chatContainer'); const nicknameInput = document.getElementById('nicknameInput'); const registerBtn = document.getElementById('registerBtn'); const displayUsername = document.getElementById('displayUsername'); const chatMessages = document.getElementById('chatMessages'); const messageInput = document.getElementById('messageInput'); const sendBtn = document.getElementById('sendBtn'); const menuBtn = document.getElementById('menuBtn'); const dropdownMenu = document.getElementById('dropdownMenu'); const myProfileModal = document.getElementById('myProfileModal'); const openMyProfileBtn = document.getElementById('openMyProfileBtn'); const closeMyProfileBtn = document.getElementById('closeMyProfileBtn'); const friendProfileModal = document.getElementById('friendProfileModal'); const chatAvatar = document.getElementById('chatAvatar'); const closeFriendProfileBtn = document.getElementById('closeFriendProfileBtn'); const pinnedPanel = document.getElementById('pinnedPanel'); const closePinnedBtn = document.getElementById('closePinnedBtn'); const settingsScreen = document.getElementById('settingsScreen'); const openSettingsBtn = document.getElementById('openSettingsBtn'); const closeSettingsBtn = document.getElementById('closeSettingsBtn');
setTimeout(() => { splashScreen.style.display = 'none'; authScreen.style.display = 'flex'; }, 3000);
registerBtn.addEventListener('click', () => { const name = nicknameInput.value.trim(); if (name !== '') { displayUsername.innerText = name; authScreen.style.display = 'none'; chatContainer.style.display = 'flex'; } else { alert('Пожалуйста, введите никнейм!'); } });
menuBtn.addEventListener('click', (e) => { e.stopPropagation(); dropdownMenu.style.display = (dropdownMenu.style.display === 'flex') ? 'none' : 'flex'; }); document.addEventListener('click', () => { dropdownMenu.style.display = 'none'; });
openMyProfileBtn.addEventListener('click', () => { myProfileModal.style.display = 'flex'; }); closeMyProfileBtn.addEventListener('click', () => { myProfileModal.style.display = 'none'; }); chatAvatar.addEventListener('click', () => { friendProfileModal.style.display = 'flex'; }); closeFriendProfileBtn.addEventListener('click', () => { friendProfileModal.style.display = 'none'; }); closePinnedBtn.addEventListener('click', () => { pinnedPanel.style.display = 'none'; });
openSettingsBtn.addEventListener('click', () => { settingsScreen.style.display = 'flex'; }); closeSettingsBtn.addEventListener('click', () => { settingsScreen.style.display = 'none'; });
function sendMessage() { const text = messageInput.value.trim(); if (text !== '') { const messageDiv = document.createElement('div'); messageDiv.classList.add('message', 'outgoing'); messageDiv.innerText = text; chatMessages.appendChild(messageDiv); messageInput.value = ''; chatMessages.scrollTop = chatMessages.scrollHeight; } }
sendBtn.addEventListener('click', sendMessage); messageInput.addEventListener('keydown', (e) => { if (e.key === 'Enter') sendMessage(); });
function changeTheme(theme) { const screen = document.getElementById('mainPhoneScreen'); const header = document.getElementById('chatHeader'); const sHeader = document.getElementById('settingsHeader'); const input = document.getElementById('inputArea'); document.querySelectorAll('.theme-dot').forEach(dot => dot.classList.remove('active')); if (theme === 'default') { screen.style.backgroundColor = '#120d24'; header.style.background = 'linear-gradient(135deg, #1d0047 0%, #5800a6 100%)'; sHeader.style.background = 'linear-gradient(135deg, #1d0047 0%, #5800a6 100%)'; input.style.backgroundColor = '#120d24'; } else if (theme === 'hacker') { screen.style.backgroundColor = '#050c05'; header.style.background = '#0a1f0a'; sHeader.style.background = '#0a1f0a'; input.style.backgroundColor = '#050c05'; } else if (theme === 'pink') { screen.style.backgroundColor = '#1a0510'; header.style.background = 'linear-gradient(135deg, #47002b 0%, #a60065 100%)'; sHeader.style.background = 'linear-gradient(135deg, #47002b 0%, #a60065 100%)'; input.style.backgroundColor = '#1a0510'; } else if (theme === 'space') { screen.style.backgroundColor = '#051020'; header.style.background = 'linear-gradient(135deg, #001f47 0%, #0058a6 100%)'; sHeader.style.background = 'linear-gradient(135deg, #001f47 0%, #0058a6 100%)'; input.style.backgroundColor = '#051020'; } else if (theme === 'black') { screen.style.backgroundColor = '#000000'; header.style.background = '#111111'; sHeader.style.background = '#111111'; input.style.backgroundColor = '#000000'; } event.target.classList.add('active'); }